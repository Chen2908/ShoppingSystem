import java.util.Date;

public class PrimumAccount extends Account {

    public PrimumAccount(String id, String billingAddress, Date open, Date close, int balance) {
        super(id, billingAddress, open, close, balance);
    }
}
